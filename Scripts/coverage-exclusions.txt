# Paths excluded from the coverage denominator (implementation plan §3.3).
#
# Each entry is a regex matched against the source path, and each needs a
# one-line reason. The file has two sections, and the distinction matters.
#
#   [not-source]  Not application code at all. Excluded because measuring it
#                 would be meaningless, not because it is trivial. Free to
#                 branch; no complexity cap applies.
#
#   [capped]      Application code excluded for being boilerplate. Subject to
#                 the complexity cap in Scripts/swiftlint-excluded.yml, which
#                 permits no branching. Exclusion here is earned by triviality,
#                 not asserted — a file that acquires a branch stops qualifying
#                 and must either be covered or have its decision extracted into
#                 a seam (D-11, specification §15.2).
#
# Adding an entry to [capped] deserves the same scrutiny as deleting a test.

[not-source]
# Test code and fixtures are not part of the measured denominator.
/Tests/
/Fixtures/

# itchyctl is a development harness and is not shipped (specification §1). It is
# outside the denominator defined in implementation plan §3.3, which covers the
# two packages and the non-view logic of the app target. It still builds under
# `make test`, so it cannot rot unnoticed.
/Harness/

[capped]
# SwiftUI view bodies: covering these asserts that a view hierarchy was
# constructed, which raises the number and finds nothing.
/App/Interface/.*View\.swift$

# NSViewRepresentable plumbing: makeNSView/updateNSView are framework contract,
# and the decisions they would otherwise make live in PadTextCoordinator seams.
/App/Platform/TextViewBridge/PadTextEditor\.swift$

# Application lifecycle stub: @main, activation policy, delegate wiring.
/App/ItchyApp\.swift$
